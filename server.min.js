const dotenv=require("dotenv");dotenv.config({path:"./config.env"});const mongoose=require("mongoose"),app=require("./app"),DB=process.env.DATABASE.replace("<PASSWORD>",process.env.DATABASE_PASSWORD);mongoose.connect(DB).then(conn=>{console.group("connected successfully")});const port=process.env.PORT||3e3;app.listen(port,()=>{console.log(`listening on port ${port}`)});